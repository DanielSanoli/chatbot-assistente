/**
 * Brain (LLM + ferramentas) — stub V0.
 * Orquestração de intenções e tools entra em features posteriores.
 */
export function createBrain() {
  return {
    name: "brain" as const,
    ready: false,
  };
}
